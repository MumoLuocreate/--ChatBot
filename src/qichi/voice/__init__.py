"""角色的嗓子：把已经写好的那句话，变成可以念出来的一段音频。

只做三件事，且都在**文字发送之后**的异步链路里跑：
1. `text`：把颜文字与 emoji 从「要念的文本」里剥干净（用户 2026-09-14 裁定，见
   `doc/TTS-实施计划-20260914.md` §2.35）；
2. `synth`：调用语音合成，拿回音频字节；
3. `cache`：按内容寻址落盘，重入不会重复合成。

明确不做：不判断语气、不选择情绪、不决定这段要不要发语音——那些归主模型
（`decision_authority.llm`），本包只负责"把字念出来"这一件事实。
"""
